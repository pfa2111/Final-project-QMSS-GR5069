# Exercise 4.10 -- Kansas City Clustered Design

rm(list=ls())       # clear objects in memory
library(ri)         # load the RI package
set.seed(1183)   # random number seed, so that results are reproducible
